# Loaded automatically by Vite in dev mode (npm run dev).
# Points the dev server (localhost:5173) at the local FastAPI backend (:8000).
# Production builds do NOT use this file; they default to same-origin requests.
VITE_API_BASE_URL=http://localhost:8000
