# Loaded automatically by Vite in production builds (npm run build).
# Empty base URL means same-origin, relative API requests. FastAPI serves the
# built assets and the API from one origin, so no backend host is baked in.
VITE_API_BASE_URL=
